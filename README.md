# pypatree

```bash
uv add --dev https://github.com/yberreby/pypatree.git
uv run pypatree
```

Example output on this very repo:
```
❯ uv run pypatree
pypatree
├── build_tree()
├── get_module_items()
├── get_packages()
├── __main__
│   ├── main()
│   └── run()
└── display
    └── render_tree()
```
